name: Benchmark progression (paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Explain paused state
        run: |
          echo "Automatic 001-006 benchmark progression is intentionally paused for the v0.4/v1 product-validation phase."
          echo "Existing historical runs remain evidence. Use V1_VALIDATION_PLAN.md for the next blind-validation stage."
          echo "Do not restart repeated tuning against the legacy benchmark suite from this workflow."
