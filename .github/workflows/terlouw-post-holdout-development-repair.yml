name: Terlouw post-holdout development repair (paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Explain paused state
        run: |
          echo "Terlouw-specific automatic repair is intentionally paused."
          echo "The original frozen holdout metrics remain historical evidence; later Terlouw runs are post-holdout development evidence only."
          echo "The v0.4 process-role architecture addresses the general shared-support activity failure class without paper-specific repair."
