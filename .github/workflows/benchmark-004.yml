name: Benchmark 004

on:
  workflow_dispatch:
    inputs:
      runs:
        description: "Number of independent LLM extraction runs"
        required: false
        default: "3"

concurrency:
  group: benchmark-004-${{ github.ref }}
  cancel-in-progress: false

jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
      actions: write
    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      OPENAI_MODEL: gpt-5-mini
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install -e ".[test]"
      - name: Deterministic tests
        run: python -m pytest -q
      - name: Verify OpenAI secret
        run: test -n "$OPENAI_API_KEY"
      - name: Run Benchmark 004
        shell: bash
        run: |
          RUNS="${{ github.event.inputs.runs }}"; [ -n "$RUNS" ] || RUNS=3
          python -m ai_lca.benchmark live \
            --expected benchmarks/gonzales_calienes_2025/expected.json \
            --source benchmarks/gonzales_calienes_2025/source_excerpt.txt \
            --runs "$RUNS" --model "$OPENAI_MODEL" \
            --output-dir benchmark_runs/gonzales_calienes_2025
      - name: Enforce standard quality gate
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          s=json.loads(Path('benchmark_runs/gonzales_calienes_2025/summary.json').read_text())
          checks=[('mean overall',s['mean_overall_score'],.85),('minimum run',s['min_overall_score'],.80),('process recall',s['mean_process_recall'],.90),('process precision',s['mean_process_precision'],.90),('flow recall',s['mean_flow_recall'],.85),('flow precision',s['mean_flow_precision'],.85)]
          failed=[f'{n} {v:.3f} < {t:.3f}' for n,v,t in checks if float(v)<t]
          print(json.dumps(s,indent=2))
          if failed:
              print('\n'.join(failed)); raise SystemExit(1)
          PY
      - name: Upload reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-004-${{ github.run_id }}
          path: benchmark_runs/gonzales_calienes_2025
          if-no-files-found: warn
          retention-days: 30
      - name: Advance to Benchmark 005
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run benchmark-005.yml --repo "$GITHUB_REPOSITORY" --ref main -f runs=3
