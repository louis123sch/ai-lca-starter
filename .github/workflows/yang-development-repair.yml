name: Yang development repair (paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Explain paused state
        run: |
          echo "Yang-specific automatic repair is intentionally paused."
          echo "Yang remains development/regression evidence, but v0.4 should not be repeatedly tuned against it."
          echo "Use the general process-role architecture and V1_VALIDATION_PLAN.md for further development."
