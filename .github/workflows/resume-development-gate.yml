name: Resume development gate (paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Explain paused state
        run: |
          echo "The legacy exact-SHA development resume gate is intentionally paused."
          echo "The old validated-repair chain has completed its purpose and should not restart Yang/holdout tuning automatically."
          echo "See V1_VALIDATION_PLAN.md for the current development and validation procedure."
